POSTGRES_DB=my_group_buying_dev
POSTGRES_USER=group_buying
POSTGRES_PASSWORD=local-development-only-password

# Next.js sets NODE_ENV automatically for dev/build/start. Production must run
# with NODE_ENV=production; do not put production secrets in this tracked file.
# NODE_ENV=development

# Optional development-only hostname (no scheme) for an HTTPS tunnel that may
# request the Next.js dev server, for example: random-name.trycloudflare.com
# NEXT_DEV_ALLOWED_ORIGIN=random-name.trycloudflare.com

# Required by Prisma and the application server. Replace this development-only
# example through the deployment platform's secret/environment configuration.
DATABASE_URL=postgresql://group_buying:local-development-only-password@localhost:5433/my_group_buying_dev

# Server-only LINE Login OAuth 2.1 / OpenID Connect configuration.
LINE_LOGIN_CHANNEL_ID=replace-channel-id
LINE_LOGIN_CHANNEL_SECRET=replace-channel-secret
LINE_LOGIN_REDIRECT_URI=https://example.trycloudflare.com/api/auth/line/callback

# ECPay convenience-store map and authoritative store-list verification.
APP_BASE_URL=http://localhost:3000
ECPAY_LOGISTICS_ENVIRONMENT=stage
ECPAY_LOGISTICS_MERCHANT_ID=0000000
ECPAY_LOGISTICS_HASH_KEY=replaceHashKey
ECPAY_LOGISTICS_HASH_IV=replaceHashIv

# Server-only S3-compatible storage for normalized Group Buy images.
# Production public URLs must use HTTPS. These values are read lazily only by
# upload/storage operations; never expose them with a NEXT_PUBLIC_ prefix.
GROUP_BUY_IMAGE_STORAGE_ENDPOINT=https://example-account.r2.cloudflarestorage.com
GROUP_BUY_IMAGE_STORAGE_REGION=auto
GROUP_BUY_IMAGE_STORAGE_BUCKET=replace-bucket
GROUP_BUY_IMAGE_STORAGE_ACCESS_KEY_ID=replace-access-key-id
GROUP_BUY_IMAGE_STORAGE_SECRET_ACCESS_KEY=replace-secret-access-key
GROUP_BUY_IMAGE_PUBLIC_BASE_URL=https://images.example.com

# E2E only. The runner sets this together with an exact disposable database URL.
# E2E_GROUP_BUY_IMAGE_STORAGE=1
